/**
 * CONSTANTS
 */
export const DAY_MSEC = 24 * 60 * 60 * 1000; // days*hours*minutes*seconds*milliseconds
export const HOUR_MSEC = 1 * 60 * 60 * 1000; // hours*minutes*seconds*milliseconds
export const MIN_MSEC = 1 * 60 * 1000;       // minutes*seconds*milliseconds
export const SEC_MSEC = 1 * 1000;            // seconds*milliseconds
export const NUM_EVENT_DATES = 30;           // days


